connectSimpleServer
===================

Node Connect Server
> a simple server that will run on port 8080 using the Node Connect module


INSTALL AND RUN
-----------------

    npm install connect
     
    node connect.js
